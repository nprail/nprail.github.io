{
  "name": "nprail-github-io",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx @11ty/eleventy && mkdir -p _site/assets/css && npx tailwindcss -i ./src/assets/css/main.css -o ./_site/assets/css/main.css --minify",
    "dev": "npx @11ty/eleventy --serve",
    "download-images": "bash scripts/download-images.sh"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@tailwindcss/typography": "^0.5.10",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0"
  }
}
